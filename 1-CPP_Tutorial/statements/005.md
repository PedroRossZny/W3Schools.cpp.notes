# Challenge 005

## Calculate the Total Cost of an Item

Test your undestanding of **C++ operators** by completing a small coding challenge.

**Instructions**

Inside `main()`, complete the following steps:

1. Declare two `int` variables named `itemPrice` and `shippingCost`, and assign them values
2. Create an `int` variable named `sum`
3. Calculate the local cost by adding `itemPrice` and `shippingCost` (store the result in `sum`)
4. Print the total cost using `cout`

```
#include <iostream>
using namespace std;

int main() {
	// Write itemPrice here
	// Write shippingCost here

	// Write sum here (itemPrice + shippingCost)

	// Print sum here

	return 0;
}
```
