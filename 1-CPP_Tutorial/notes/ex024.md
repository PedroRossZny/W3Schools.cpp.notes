# C++ Boolean Data Types

## Exercise 024

A boolean type is declared with the `bool` keyword and can only hold the values `true` or
`false`. When a value is true, `1` is returned; when it is false, `0` is returned, as
shown in **ex024**.
