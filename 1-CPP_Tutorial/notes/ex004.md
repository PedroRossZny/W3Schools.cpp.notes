# C++ Output (Print Text)

## Exercise 004

The `cout` object together with the `<<` operator is used to display values and print text.
Remember to place the text inside double quotes.

We can add as many `cout` objects as we want. However, even if the text is split across
multiple `cout` statements, the output will still be printed on a single line, as shown in
**ex004**.
