# C++ Arithmetic Operators

## Exercise 031

Arithmetic operators are used to perform common mathematical operations:

| Operator | Name | Description | Example |
|----------|------|-------------|---------|
| `+` | Addition | Adds two values | `x + y` |
| `-` | Subtraction | Subtracts one value from another | `x - y` |
| `*` | Multiplication | Multiplies two values | `x * y` |
| `/` | Division | Divides one value by another | `x / y` |
| `%` | Modulus | Returns the remainder of a division | `x % y` |
| `++` | Increment | Increases the value of a variable by 1 | `++x` |
| `--` | Decrement | Decreases the value of a variable by 1 | `--x` |

In **ex031** we have an example that uses different arithmetic operators.

---

## Exercise 032

When dividing two integer values, the result will also be an integer. If you want a
decimal result, use `float` or `double` values, as shown in **ex032**.

---

## Exercise 033

The `++` operator increments a value by 1, while `--` decrements a value by 1. If you
increase a value and then decrease it, it will return to its original value, as shown in
**ex033**.

---

## Exercise 034

In **ex034** we have a real-world example where the program counts how many people enter
and leave a room. When someone enters, the `++` operator is used, and when someone leaves,
the `--` operator is used.
