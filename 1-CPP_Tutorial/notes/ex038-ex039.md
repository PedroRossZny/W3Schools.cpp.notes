# C++ Comparison Operators

## Exercise 038

Comparison operators are used to compare two values or variables. The return value of a
comparison is `1` for true and `0` for false, values known as booleans. The full list of
comparison operators is:

| Operator | Name | Example |
|----------|------|---------|
| `==` | Equal to | `x == y` |
| `!=` | Not equal to | `x != y` |
| `>` | Greater than | `x > y` |
| `<` | Less than | `x < y` |
| `>=` | Greater than or equal to | `x >= y` |
| `<=` | Less than or equal to | `x <= y` |

As shown in **ex038**.

---

## Exercise 039

Comparison operators are frequently used in real-world situations, such as checking
whether a person is old enough to vote, or whether a password is long enough, as shown in
**ex039**.
