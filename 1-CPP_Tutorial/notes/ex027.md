# C++ auto

## Exercise 027

The `auto` keyword automatically detects the type of a variable based on its assigned
value. However, it only works when a value is assigned at the time of declaration — you
cannot use it without providing a value. Another detail is that once the type is inferred,
it will remain the same, as shown in **ex027**.
