# C++ Output Numbers

## Exercise 005

`cout` can also be used to print numbers. Unlike text, numbers are not placed inside double
quotes.

We can print a number directly, or we can perform mathematical operations such as addition
`+` or multiplication `*`, and the result of those operations will be printed, as shown in
**ex005**.
